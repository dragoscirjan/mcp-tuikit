# @dragoscirjan/mcp-tuikit-flow-engine

The declarative YAML flow execution engine for automating terminal sequences within `mcp-tuikit`. It parses predefined execution scripts, allowing AI agents or CI environments to orchestrate complex multi-step interactions (e.g., sending keys, waiting for patterns, capturing artifacts) in a single operation.
