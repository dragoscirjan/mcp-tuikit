# @dragoscirjan/mcp-tuikit-linux-utils

Linux-specific utilities for `mcp-tuikit`. This module manages virtual compositors and headless window environments (such as `Xvfb` for X11, and `sway` or `kwin` for Wayland), enabling true headless execution of native terminal emulators in CI/CD pipelines.
